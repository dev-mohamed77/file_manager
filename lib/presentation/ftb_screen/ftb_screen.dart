import 'package:flutter/material.dart';

class FTBScreen extends StatelessWidget {
  const FTBScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: const [],
      ),
    );
  }
}
